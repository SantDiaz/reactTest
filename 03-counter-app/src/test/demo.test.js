describe ( ' pruebas en el archivo demo.test.js', () => {   // Esta funcion describe el titulo para que se vea reflejada en la terminal      
    
    test('deben de ser iguales los string', () => {        // Esta es la funci
    
        const isActive = true;
        
        if (!isActive){
    
            throw new Error ('No esta activo')
    
        }
     })


})
